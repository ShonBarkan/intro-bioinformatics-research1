# Configuration for Netlify deployment
[build]
  # The directory where the React application resides
  base = "client"

  # The actual build command to execute inside the client directory
  command = "npm run build"

  # The output directory generated by Vite (relative to the base directory)
  publish = "dist"